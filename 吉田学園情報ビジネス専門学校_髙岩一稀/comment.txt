# コード
 # 1
	ゲームとしてはうまくまとまっていると思います。
	数学を勉強して敵の移動や弾道で表現が増すと面白くかつ評価しやすいものになります。

 # 2
	if (g_pVtxBuffEffect != NULL)
	{
		g_pVtxBuffEffect->Release();
		g_pVtxBuffEffect = NULL;
	}
		↓
	#define SAFE_RELEASE(a)	{ if( a ){ a->Release(); a = NULL; } }

 # 3
    グローバル変数を使いまくってるのでmalloc/freeを覚えて
	cppごとに必要な変数を作って開放する練習するとベスト。

 # 4
	MOVE_PLAYERなどマクロで移動量を持ってるところは
	テキスト読み込みなどで自分以外が調整可能にすると
	チーム作業を想定してることも分かるのでベスト。

 # 5
	ソースが増えてくる場合はフォルダを作って分けていくのもいいかも。
